{
"btcd" : [
        {
                "url": "${APP_BITCOIN_NODE_IP}:${APP_BITCOIN_RPC_PORT}",
                "auth": "${APP_BITCOIN_RPC_USER}",
                "pass": "${APP_BITCOIN_RPC_PASS}",
                "notify" : true
        }
],
"btcsig" : "/mined by PupaPool on Umbrel/",
"mindiff": 1000000,
"startdiff": 1000000,
"maxdiff" : 1000000,
"logdir" : "/www",
"zmqblock" : "tcp://${APP_BITCOIN_NODE_IP}:${APP_BITCOIN_ZMQ_HASHBLOCK_PORT}"
}
